import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import TeamderApp from './TeamderApp';
import reportWebVitals from './reportWebVitals';

ReactDOM.render(
  <React.StrictMode>
    <TeamderApp />
  </React.StrictMode>,
  document.getElementById('root')
);

reportWebVitals();
